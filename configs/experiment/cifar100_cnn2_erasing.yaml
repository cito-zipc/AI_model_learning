# @package _global_
defaults:
  - override /data: cifar100
  - override /model: cifar100_cnn2

experiment_name: cifar100_cnn2_erasing


logger:
  tensorboard:
    name: cifar100_cnn2_erasing   

data:
  use_erasing: true
tags: ["cifar100", "cnn2", "erasing"]
