# @package _global_
defaults:
  - override /data: cifar100
  - override /model: cifar100_net4

experiment_name: cifar100_net4_erasing_epoch100


logger:
  tensorboard:
    name: cifar100_net4_erasing_epoch100   

data:
  use_erasing: true
trainer:
  max_epochs: 100
tags: ["cifar100", "net4", "erasing"]
