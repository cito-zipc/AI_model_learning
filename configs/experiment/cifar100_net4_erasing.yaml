# @package _global_
defaults:
  - override /data: cifar100
  - override /model: cifar100_net4

experiment_name: cifar100_net4_erasing


logger:
  tensorboard:
    name: cifar100_net4_erasing   

data:
  use_erasing: true
tags: ["cifar100", "net4", "erasing"]
