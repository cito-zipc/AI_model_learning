# @package _global_
defaults:
  - override /data: cifar100
  - override /model: cifar100_cnn2

experiment_name: cifar100_cnn2_test


logger:
  tensorboard:
    name: cifar100_cnn2_test   

trainer:
  max_epochs: 10
tags: ["cifar100", "cnn2"]
