# @package _global_
defaults:
  - override /data: cifar100
  - override /model: cifar100_net4

experiment_name: cifar100_net4_test


logger:
  tensorboard:
    name: cifar100_net4_test   

trainer:
  max_epochs: 10
tags: ["cifar100", "net4"]
